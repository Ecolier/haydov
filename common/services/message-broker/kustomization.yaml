configMapGenerator:
  - name: message-broker-config
    envs:
      - .env
secretGenerator:
  - name: message-broker-credentials
    envs:
      - .env.secret
generatorOptions:
  disableNameSuffixHash: true
